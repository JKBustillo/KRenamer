/** Temas disponibles en KRenamer. Ver `src/theme.css`. */
export type Theme = "sumi" | "terminal";

/** Tema por defecto la primera vez que se abre la app. */
export const DEFAULT_THEME: Theme = "sumi";
